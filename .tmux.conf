set -g status-key vi
set -g mouse-select-pane on
set -g mouse-select-window on
set -g mouse-resize-pane on

setw -g mode-key vi
setw -g mode-mouse on

unbind C-b
set -g prefix M-a
bind M-a send-prefix

source '/home/tsurai/.powerline/powerline/bindings/tmux/powerline.conf'
